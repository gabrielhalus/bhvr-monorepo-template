import * as RAspect from "@radix-ui/react-aspect-ratio";

/**
 * AspectRatio — constrains content to a ratio. Re-exported bare from Radix.
 *
 *   <AspectRatio ratio={16 / 9}><img … /></AspectRatio>
 */
export const AspectRatio = RAspect.Root;
